/** When there is no session: short delay so content does not flash under the header on first paint. */
export const TONGUE_NO_SESSION_BOOT_MS = 400;

/** “New chat” shell duration (aligned with the initial boot feel). */
export const TONGUE_NEW_CHAT_SHELL_MS = 400;
